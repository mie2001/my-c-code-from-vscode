//主函数已经给定,请补齐程序,使函数SumDiff完成求两个整数的和与差(函数返回和值,差值通过设定指针形式参数通过间接引用进行计算),提交完整的程序代码
#include <stdio.h>
#include <stdlib.h>
int sumDiff(int *a, int *b);
int main()
{
    printf("\n");
    scanf("%d %d\n");
    int sumDiff(&a, &b);
}
int sumDiff(int *a, int *b)
{
    int sum;
    int diff;
}